<h1>All results</h1>

<%=
  container_id = rand(1_000_000_000)
  render partial: "polling", locals: {
    path: "/poll/total/#{@poll.id}",
    interval: 3000,
    container_id: container_id
  }
%>

<div id="<%= container_id %>">Loading...</div>

<br/>
<br/>
<br/>
<%= link_to "Create new poll", "/poll" %>
